version: '3.9'
services:
  swagger-editor:
    image: swaggerapi/swagger-editor:v4.1.2
    container_name: swagger-editor
    environment:
      SWAGGER_FILE: /tmp/api/storage-api.yaml
    volumes:
      - ./src/main/storage-api.yaml:/tmp/api/storage-api.yaml
    ports:
      - '10000:8080'